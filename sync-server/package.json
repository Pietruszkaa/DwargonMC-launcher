{
  "name": "dwargonmc-sync-server",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone read-only backend for DwargonMC Launcher file sync, backgrounds, health, and map proxy.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "manifest": "node generate-manifest.js"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/rate-limit": "^10.1.1",
    "fastify": "^5.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
